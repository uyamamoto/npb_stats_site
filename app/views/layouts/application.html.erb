<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "NPB成績データベース" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">

    <!-- Description Meta Tag -->
    <meta name="description" content="日本プロ野球(NPB)の2024年度成績データベースです。選手の名前を入力して詳細な成績情報を簡単に取得できます。">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="<%= asset_path 'custom_icon.png' %>" type="image/png">
  <link rel="apple-touch-icon" href="<%= asset_path 'custom_icon.png' %>">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
